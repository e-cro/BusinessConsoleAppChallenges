using System;
using _02_Claims;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace _02_ClaimsTests
{
    [TestClass]
    public class ClaimRepoTests
    {
        ClaimRepo _repoTest = new ClaimRepo();
        Queue<Claim> = new Queue<Claim>();

        [TestMethod]
        public Queue<Claim> GetClaimQueue_Test()
        {
        }
    }
}
